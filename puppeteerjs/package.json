{
  "dependencies": {
    "puppeteer": "^15.1.1"
  },
  "devDependencies": {
    "jest": "^28.1.3",
    "jest-puppeteer": "^6.1.1",
    "prettier": "2.7.1"
  },
  "scripts": {
    "test": "jest -i",
    "prettier-watch": "onchange \"**/*\" -- prettier --write --ignore-unknown {{changed}}"
  }
}
